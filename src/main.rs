extern crate clap;

mod mbc0;
mod cpu;
mod gb;
mod memory;
mod mmu;
mod vram;
mod wram;

use std::env;

fn main() {
    println!("Hello, world!");
    // let mut gb = match gb::Gameboy::power_on()
    // {

    // };
}